[package]
name = "blacknode-ros2"
version = "0.1.0"
description = "ROS 2 topics, services, visual tests, and a safety-gated SO-ARM101 LeRobot bridge."
requires-blacknode = ">=0.1.0"

[categories]
"ROS 2" = "#2e9fe6"

[dependencies]
pip = ["PyYAML>=6", "roslibpy>=1.5"]
imports = ["yaml", "roslibpy"]
docker = ["ros:jazzy"]
